spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/counter-db
    username: postgres
    password: password
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true